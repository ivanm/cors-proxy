{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "CORS proxy",
  "scripts": {
    "deploy-dev": "sls --stage dev deploy",
    "offline-dev": "sls --stage dev offline --httpPort 9090"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "serverless": "^2.1.1"
  },
  "devDependencies": {
    "serverless-offline": "^6.7.0"
  }
}
